如果 $ARGUMENTS 为空，先 ls projects/ 列出可用项目让用户选择，不要猜测项目名。

按 context 执行交互大图，快速模式。
并行读取 projects/$ARGUMENTS/context.md + scene-list.md + interaction-map SKILL.md + references/templates-quickref.md，然后按 Step A 开始。
不要探索目录结构，不要 ls/find，直接开始。
本命令已激活快速模式，SKILL.md 逐步确认硬停规则不生效，连续执行不暂停。
