如果 $ARGUMENTS 为空，先 ls projects/ 列出可用项目让用户选择，不要猜测项目名。

按 context 执行可交互原型，快速模式。
并行读取 projects/$ARGUMENTS/context.md + scene-list.md + prototype SKILL.md + references/prototype-templates.html，然后按 Step A 开始。
不要探索目录结构。
本命令已激活快速模式，SKILL.md 逐步确认硬停规则不生效，连续执行不暂停。
