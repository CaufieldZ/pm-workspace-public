# demos/ — 视觉样本（模型禁读）

本目录下 HTML 文件仅作浏览器视觉样本，**模型禁止用 Read 工具加载**（单文件 12-17K tokens）。

- `proto-sample.html` — 原型视觉样本
- `ppt-sample.html` — PPT 视觉样本
- `imap-sample.html` — 交互大图视觉样本

需要查看效果：`open path/to/sample.html` 用浏览器打开。
需要参考样式规范：读 `../anti-ai-slop.md` 和 `../tokens.css`。
