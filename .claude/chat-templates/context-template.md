# {项目名} · Context

> 本文件由 Chat Opus 在方案讨论后输出，本地模型以此为权威上下文。
> 除第 8 章执行计划外，本地模型默认只读（用户明确指示时可追加条目）。
>
> **双轨操作差异：**
> - Chat 轨：每个产物完成后，Claude 更新第 8 章的执行进度和已交付状态。
> - Code 轨：Claude Code 通过 `ls deliverables/` 直接读文件系统获取状态，不修改 context.md（除用户明确指示追加第 5/6/7 章条目外）。

## 1. 项目元信息

| 字段 | 值 |
|------|-----|
| 项目名 | |
| 版本 | v1 |
| 日期 | |
| 状态 | 进行中 |
| 产品线 | |
| 涉及端 | App / Web / CMS |

## 2. 产品现状

（Chat Opus 填写：各端现有页面结构、现有能力、用户可见的现状。
本地模型需要知道"从哪改"，所以这一章要写清楚改之前长什么样。）

## 3. 用户角色

> 列出本项目涉及的所有角色及权限边界。

| 角色 | 描述 | 关键权限/限制 |
|------|------|-------------|
| （示例）普通用户 | 已完成 KYC 的注册用户 | 可交易、可提币，单日限额 X |

## 4. 场景编号

> 编号锁定后全局不可改，新增只追加。所有产出物引用此编号。

| 编号 | 场景名 | 端 | 设备 | 触发条件 | 优先级 |
|------|--------|-----|------|---------|--------|
| A-1 | （示例） | App | 📱phone | | P0 |

## 5. 术语表

> 全链路产出物必须严格使用以下术语，禁止自造别名。

| 术语 | 定义 | 英文/缩写 |
|------|------|----------|
| （示例）挂单 | 用户提交但未成交的委托单 | Open Order |

## 6. 业务规则

> 状态机、公式、限制条件、合规约束。本地模型在 PRD/行为规格中严格遵守。

（Chat Opus 填写。格式不限：表格、状态机描述、条件列表均可。）

## 7. 设计决策

> Chat Opus 在方案讨论中敲定的决策记录，含竞品参考。
> 本地模型遇到歧义以此为准。

| 决策项 | 结论 | 理由 | 竞品参考 |
|--------|------|------|---------|
| （示例）交互方案 A vs B | A | 用户路径更短 | Binance 同方案 |

## 8. 执行计划

> Chat Opus 在方案讨论后输出。告诉本地模型该怎么跑。
> 含阻塞项和外部依赖（等人确认的事项）。

- **链路类型**：（简单 / 复杂 / 超复杂）
- **执行顺序**：（如：scene-list → interaction-map → prototype → prd）
- **跳过步骤**：（如：requirement-framework——本项目不需要）
- **注意事项**：
  - （示例）合约页拆分为 F-0a / F-0b / F-0c 三个子场景

**阻塞项 & 外部依赖：**

| 事项 | 负责人 | 状态 |
|------|--------|------|
| （示例）和后端确认 xx 接口字段 | @后端 | 待确认 |

> 阻塞项状态为「待确认」时，本地模型跳过相关部分并在产出物中标注。

## 9. 变更记录

> 每轮迭代的变更追溯。新 session 接手项目时先看这里了解上下文。

| 版本 | 日期 | 变更内容 | 原因 |
|------|------|---------|------|
| v1 | {日期} | 初始版本 | — |
